# 6-8k 全栈/后端岗位面试学习目录

## 使用说明

这份目录根据目标岗位的职责和技术栈编排，目标是准备：

- 财务/ERP 内部业务系统开发与维护
- 第三方 API 对接、数据清洗、入库和定时同步
- Python 后端与基础全栈开发
- MySQL/PostgreSQL、Redis、Docker、Nginx、Git
- 使用 Cursor、Claude Code 等 AI 工具完成开发并进行人工校验

本岗位还涉及 ERP、领星和金蝶。当前目标不是成为财务软件实施顾问，而是先理解它们管理什么业务、数据如何流转、开放 API 如何对接，以及开发人员如何排查同步问题。

学习每个主题时，要求 Codex 按以下格式讲解：

1. 先讲概念和实际业务场景。
2. 给出最小可运行代码。
3. 解释每一行关键代码。
4. 给出常见错误和排查方法。
5. 给出面试官可能的追问和参考回答。
6. 最后布置一道小练习，并检查我的答案。

## P0：明天面试前必须掌握

### 1. 岗位业务理解

- 财务系统、ERP、订单系统、物流系统的基本关系
- 客户、供应商、商品、订单、付款、发票、物流等常见实体
- 内部系统开发和互联网产品开发的区别
- CRUD、审批流、状态机、操作日志、数据权限
- 业务需求如何拆成数据表、接口和前端页面
- 如何向面试官说明自己使用 AI 工具开发，但仍能负责代码质量

### 1.1 ERP 基础概念

- ERP（企业资源计划）解决什么问题
- 财务、采购、销售、库存、仓储、物流、生产模块之间的关系
- 主数据：客户、供应商、商品、仓库、组织、币种、税率
- 业务单据：采购单、销售单、入库单、出库单、调拨单、退货单、发票、收付款单
- 单据状态：草稿、提交、审核、执行、完成、关闭、作废
- 业务单据和财务凭证的关系
- 应收、应付、收入、成本、利润的基本概念
- 库存数量、可用库存、锁定库存和在途库存
- ERP 中为什么强调权限、审批、审计和数据准确性
- ERP 与普通 CRUD 系统的区别

### 1.2 领星（跨境电商 ERP）

- 领星主要服务的业务：亚马逊等跨境电商店铺运营
- 店铺、站点、Listing、SKU、ASIN、订单、库存、广告和利润的关系
- FBA、FBM、海外仓和本地仓的基本区别
- 销售订单、发货、库存变化和平台回款的数据链路
- 销售额、平台费用、广告费、物流费、采购成本和利润的关系
- 多店铺、多国家、多币种、多时区带来的数据问题
- 领星开放 API 的常见对接方式和认证思路
- 拉取订单、商品、库存、广告或利润数据时的分页与增量同步
- 平台数据延迟、重复拉取、缺失数据和接口限流
- 领星数据同步到公司财务系统/金蝶时如何做字段映射

### 1.3 金蝶基础

- 金蝶的定位：企业财务与经营管理软件/ERP
- 金蝶云、金蝶 KIS、金蝶 K/3 等产品线只需了解基本定位
- 组织、账套、会计期间和核算维度
- 客户、供应商、物料、科目等基础资料
- 销售、采购、库存和财务模块的关系
- 总账、应收、应付、出纳、固定资产的基本作用
- 业务单据如何生成财务凭证
- 借方、贷方、科目、金额和凭证审核的基本概念
- 金蝶开放平台/API、第三方接口和数据导入导出的基本思路
- 对接金蝶时的编码映射、重复提交、凭证状态和失败补偿
- 为什么财务数据不能随意修改，为什么需要反审核、红冲或冲销

### 1.4 ERP 对接面试题

- 领星订单如何同步到内部 ERP
- 订单、库存和财务数据出现不一致时如何定位
- 如何设计同步记录表和同步游标
- 第三方接口重复返回同一订单怎么办
- 金额、币种、税率和汇率如何处理
- 如何处理跨时区的订单时间和结算时间
- 接口成功但本地入库失败时如何补偿
- 财务凭证已经生成后，原始订单发生变化怎么办
- 如何保留原始响应，支持审计和问题复盘
- 如果从未使用过领星/金蝶，面试时如何诚实回答

### 2. Python 后端基础

- Python 数据类型、函数、类、异常处理
- 列表/字典推导式、迭代器、生成器
- 装饰器、上下文管理器、类型注解
- 虚拟环境、依赖管理、项目目录组织
- 日志记录、配置管理、环境变量
- 同步与异步的区别，`async/await` 的适用场景
- 常见 HTTP 状态码和请求方法

### 3. FastAPI / Flask / Django 对比

- FastAPI 路由、请求参数、响应模型
- Pydantic 数据校验和序列化
- 依赖注入和鉴权依赖
- 统一异常处理和接口文档
- Flask 的路由、蓝图、中间件和扩展
- Django 的 URL、View、Model、ORM、Migration
- 三种框架的定位和选型回答

### 4. SQL 与关系型数据库

- 表、主键、外键、唯一约束、非空约束
- `SELECT`、`INSERT`、`UPDATE`、`DELETE`
- `INNER JOIN`、`LEFT JOIN`、聚合和子查询
- 索引原理、联合索引和最左匹配
- `EXPLAIN` 基础使用
- 事务、提交、回滚
- MySQL 与 PostgreSQL 的常见区别
- 分页查询、排序、模糊查询优化
- 订单/预约/任务状态表设计
- 如何防止重复写入和脏数据

### 5. 第三方 REST API 对接

- API 鉴权：API Key、Bearer Token、签名
- 请求超时、连接错误、HTTP 错误的区别
- 参数校验、响应解析和字段映射
- 超时重试、指数退避和最大重试次数
- 幂等键、唯一约束和重复请求处理
- 分页拉取、增量同步和全量同步
- 限流、批量请求和接口版本变化
- 错误记录、失败补偿和人工重跑

### 6. Redis 基础

- String、Hash、List、Set、Sorted Set
- 缓存、会话、验证码、计数器、任务状态的使用场景
- 缓存穿透、击穿、雪崩
- TTL 和缓存更新策略
- Redis 与数据库一致性
- 基础分布式锁及其风险
- 什么数据不应该只存 Redis

### 7. Git 协作

- `clone`、`branch`、`switch`、`add`、`commit`、`pull`、`push`
- 查看 diff、日志和历史版本
- 合并冲突处理
- 如何写清晰的提交信息
- 代码评审时重点检查什么

### 8. Docker / Nginx / Linux 基础

- Dockerfile、镜像、容器、端口和挂载
- `docker build`、`docker run`、`docker compose`
- 服务无法启动时如何查看日志
- Linux 进程、端口、磁盘和日志排查
- Nginx 反向代理、静态文件和端口转发
- 前端、后端、数据库容器的基本连接关系

## P1：面试高概率深挖

### 9. 业务系统设计

- RBAC：用户、角色、权限、数据范围
- 审批流与状态机设计
- 订单状态不可逆和异常状态处理
- 操作日志与审计日志
- 创建、提交、驳回、再提交、审核通过的接口设计
- 并发修改、乐观锁和版本号
- 删除、软删除和数据留痕
- 权限校验应放在哪一层

### 10. 数据同步系统

- 同步任务的整体架构
- 定时任务和手动触发
- 游标、更新时间、版本号等增量依据
- 数据清洗、类型转换和字段兼容
- 批量插入、事务边界和性能
- 幂等 upsert 设计
- 失败重试、死信记录和补偿任务
- 同步结果统计和告警
- 如何保证数据准确性与及时性

### 11. 测试与调试

- 单元测试、接口测试、端到端测试的区别
- Pytest 基本写法
- Mock 第三方 API
- 正常、边界、异常和重复请求用例
- 通过日志定位问题
- 如何复现线上 Bug
- 如何验证 AI 生成代码没有隐藏缺陷

### 12. 前端配合能力

- Vue 组件、Props、Emit 和基础状态管理
- 表单校验、列表、分页、筛选和详情页
- 前端调用 REST API
- 登录态、Token 和权限菜单
- 常见跨域问题
- 前后端联调流程

### 13. Go 入门应答范围

- Go 的变量、函数、结构体、接口
- 指针、切片、Map 和错误处理
- Goroutine、Channel 的基本概念
- HTTP 服务和 JSON 请求处理
- Go 与 Python 的主要差异
- 如何诚实回答“有没有 Go 生产经验”

## P2：结合简历准备项目追问

### 14. 工作智能分发平台

- 客户—品牌—店铺—项目—阶段—任务的数据模型
- 任务模板如何生成实际任务
- 任务状态如何流转
- 负责人、审核人和数据范围权限
- 驳回原因必填和已完成任务不可修改
- 审计日志如何设计
- 通知、逾期和定时任务
- SQLite 与 PostgreSQL 的迁移差异

### 15. 摄影预约平台

- 用户、摄影师、套餐、预约、订单、作品、评价表设计
- 预约冲突和时间段锁定
- 接单、改期、取消和退款状态
- 支付结果回调和幂等
- WebSocket 实时沟通与断线处理
- JWT、RBAC 和接口权限
- 为什么选择 FastAPI、SQLAlchemy 和 Redis

### 16. Agent / RAG 项目

- LLM 负责什么，规则代码负责什么
- 工具注册、参数校验和工具调用失败
- proposal-only 和高风险操作确认
- 会话状态、槽位和任务恢复
- BM25、向量检索和 RRF 混合检索
- 引用约束、无依据拒答和幻觉控制
- Trace、延迟和失败步骤记录
- 如何测试 Agent，而不是只看一次对话效果

### 17. 计算机视觉项目

- 训练集、验证集、测试集
- YOLO 的输入输出和置信度
- Precision、Recall、mAP
- 小目标和类别不均衡问题
- ONNX 导出和推理部署
- FastAPI `/detect` 接口设计
- 模型版本、超时和错误返回

## P0 代码练习清单

让 Codex 逐题带练，不要只看答案：

1. 用 FastAPI 写一个用户 CRUD 接口。
2. 设计用户、角色和订单三张表，并写查询订单列表的 SQL。
3. 写一个带参数校验和统一错误返回的接口。
4. 调用第三方 API，加入超时、重试和日志。
5. 实现一个幂等的数据同步函数。
6. 使用 Redis 缓存一个查询接口，并处理缓存未命中。
7. 为订单状态流转写校验逻辑。
8. 写一个 Dockerfile 启动 Python 服务。
9. 写一段 Nginx 反向代理配置并解释每项含义。
10. 为上述接口补充 Pytest 测试。

## 面试表达训练

### 自我介绍

- 控制在 90 秒到 2 分钟
- 先讲 Python 后端和业务系统，再讲 Agent/CV
- 只选择一个项目深入，不要罗列所有项目
- 明确说明 AI 工具用于拆解、生成、调试和测试辅助

### 项目回答模板

1. 为什么做这个系统？
2. 业务流程是什么？
3. 数据表和接口如何设计？
4. 最难的问题是什么？
5. 你具体负责哪部分？
6. 出错时如何排查？
7. 如何测试和上线？
8. 如果重做，你会改进什么？

### 技术边界回答

- 不会的技术不要编造生产经验。
- 可以说明：项目主栈是 Python/FastAPI，Go 或 Django 正在补齐。
- 把重点放在通用能力：HTTP、数据库、事务、权限、日志、测试和调试。
- 对简历中无法展开的高级词汇，主动收窄到自己真正能解释的实现。

## 最后冲刺顺序

1. Python/FastAPI 接口和异常处理
2. SQL、索引、事务、表设计
3. 第三方 API、幂等、重试、数据同步
4. Redis 缓存和一致性
5. Docker、Nginx、Linux 排错
6. 工作分发平台或摄影预约平台项目复盘
7. 30 分钟模拟技术面试
8. 复习 Go/Django/Flask 的基础概念和诚实应答方式
